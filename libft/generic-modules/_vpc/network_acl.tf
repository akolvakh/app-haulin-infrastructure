#TBD 
# public subnet should only allow from external world incoming 443 and 80(or only 443?),
# in dev/stagign from VPNed ip range , in prod from world after launch
# configuration  var.ip_whitelist_cidr_blocks