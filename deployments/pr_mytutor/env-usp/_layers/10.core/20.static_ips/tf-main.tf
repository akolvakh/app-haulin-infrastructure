#--------------------------------------------------------------
# Notes
#--------------------------------------------------------------
#maximum number of EIPs per account - 5

#--------------------------------------------------------------
# EIPs
#--------------------------------------------------------------
resource "aws_eip" "nlb" {
  count = 2
  vpc   = true
  lifecycle {
    prevent_destroy = false
  }
  tags = module.label.tags
}